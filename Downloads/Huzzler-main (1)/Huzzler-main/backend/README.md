# Backend (Zuntraa)

1. Copy `.env.example` to `.env` and fill values (MONGO_URI, SENDGRID_API_KEY, JWT_SECRET, FROM_EMAIL).
2. Install dependencies: `npm install`
3. Run: `npm run dev`
4. API endpoints under `/api/auth` (register, verify-otp, resend-otp, details1, details2, login)
